{
  "name": "twino",
  "version": "1.0.0",
  "description": "It is 5 o'clock somewhere. ",
  "scripts": {
    "start": "cd server && node index.js",
    "build:server": "cd server && npm install && node index.js &",
    "build:client": "ls && cd client && ls && npm install && npm run build",
    "build": "cd client && npm run build",
    "install:client": "ls && cd client && ls && npm install",
    "install:server": "ls && cd server && ls && npm install",
    "install": "npm run install:client && npm run install:server"
  },
  "author": "Carlos Mendoza"
}
